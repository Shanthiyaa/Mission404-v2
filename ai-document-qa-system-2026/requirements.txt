# ── Lightweight / frequently-changed API dependencies ─────────────────────────
# Heavy packages (torch, faiss, sentence-transformers etc.) are in requirements-base.txt
# which is installed as a separate cached Docker layer.

# ── API server ────────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
python-dotenv>=1.0.0
psycopg[binary]>=3.1.0
PyJWT>=2.8.0
bcrypt>=4.1.0
sqlalchemy>=2.0.0
